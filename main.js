/* Primeira Forma */

const oneStars = 2, twoStars = 15,treeStars = 18, fourStars = 25, fiveStars = 40;

let media = ((oneStars * 1) + (twoStars * 2) + (treeStars * 3) + (fourStars * 4) + (fiveStars * 5)) / (oneStars + twoStars + treeStars + fourStars + fiveStars);

console.log("O resultado da média foi de " + media + " estrelas.");
